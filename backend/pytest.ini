[pytest]
# Tell pytest where to find tests and how to discover them.
# -v : verbose output (shows each test name and result)
# -s : don't capture print statements (useful for debugging)
testpaths = tests
addopts = -v
